<%= link_to  "Add New", new_entrepreneurblog_path, class:'btn btn-primary pull-right' %>
 <hr>
<div class="row">
  <% if @eblogs.exists?%>
  <% @eblogs.each do |blog|%>
    <div class="col-md-3">
        <h4><%= link_to  "#{blog.title}", entrepreneurblog_path(blog) %><h4>
        <%= image_tag  "#{blog.coverpicture}", width:'50%' %>
    </div>
  <%end%>
<% else %>
<p>No post available</p>
<% end %>
  
</div>